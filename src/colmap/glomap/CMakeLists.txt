# Copyright (c), ETH Zurich and UNC Chapel Hill.
# All rights reserved.
#
# Parallel fork-style scene layer (namespace colmap::glomap). Populated by
# section-04 (scene types) and beyond. INTERFACE until sources land.

set(FOLDER_NAME "glomap")

COLMAP_ADD_LIBRARY(
    NAME colmap_glomap
    TYPE INTERFACE
    SRCS
)
