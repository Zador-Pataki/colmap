#include "colmap/glomap/track.h"

// Intentionally empty — Track is currently a passive data struct with no
// non-inline methods. Present so CMake has a translation unit per type to
// satisfy `COLMAP_ADD_LIBRARY` expectations.

namespace colmap::glomap {}  // namespace colmap::glomap
